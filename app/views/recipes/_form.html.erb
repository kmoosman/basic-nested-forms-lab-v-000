<%= form_for @recipe do |f| %>
  Title: <%= f.text_field :title %>
  <br />

  <%= f.fields_for :ingredients do |i| %>
    <%= i.label :name %>
    <%= i.text_field :name %>

    <%= i.label :quantity_0 %>
    <%= i.text_field :quantity_0%>
    <br/>
  <% end %>

  <%= f.submit %>
<% end %>
